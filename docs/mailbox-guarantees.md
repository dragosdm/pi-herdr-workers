# Mailbox guarantees

This matrix covers the 40 executing Phase 1 through Phase 3 cases, not the planned process-interruption suite. Run it with:

```sh
npm ci
node --import tsx --test tests/extensions/herdr-worker-pi-compat.test.ts tests/extensions/herdr-worker-mailbox.test.ts
```

The production transport publishes JSON through a synchronous temporary-file write and rename. The worker extension validates the claimed sender pane and invokes Pi's custom-message API. The transport retains the file while that filename is in flight. The `context` and `agent_settled` hooks acknowledge matching custom entries visible in SessionManager memory. Lifecycle entries also pass through the extension's existing handled/retry gate before cleanup.

A send receipt proves publication, not receiver acceptance or execution. A matching ordinary entry on a fresh receiver permits cleanup without reading the envelope again. In an existing receiver, a drain skips in-flight filenames before looking for matching entries, so the acknowledgement hooks still matter.

## Evidence limits

The controlled host separates its queue, memory entries, and a test JSONL file. The default `explicit` mode preserves separate consumption, append, write, reopen, and hook operations. Named `file-backed`, `deferred-first-write`, `disabled`, and `write-failed` modes model the storage boundaries for later fault tests. A deferred flush is explicit; a modeled write error occurs after memory insertion without rolling it back. These modes have fixture tests, but their JSONL is not Pi's session-file format. File-only reconstruction in the Phase 1 cases uses a fresh host in the same test process after stopping the previous receiver, not a process kill.

The Phase 1 acknowledgement cases deliberately write and reopen the test file before invoking the hook. That order proves those cases, not a requirement enforced by production. `getEntries()` does not prove a successful disk write. The real-Pi cases now execute the opposite order as known gaps. No case claims universal no-loss, exactly-once delivery, power-loss durability, or assignment completion.

Filesystem operations run under a private temporary root. Most watch and poll callbacks are captured and invoked explicitly. One bounded smoke test uses real `fs.watch` with polling disabled. Another makes watch creation throw, then invokes the poll callback. Fixtures dispose listeners and subscriptions, release blocked deliveries and assistant streams, dispose Pi sessions, reap their owned liveness child, and remove their roots. These tests need no Herdr panes, credentials, provider requests, or global Pi installation. Process-kill recovery remains outside this matrix.

## Scheduling, rejection, and containment

The real-watch smoke test has an unresolved intermittent timeout on the September 6, 2026 macOS verification run. Watch creation returned, but no notification arrived within the unchanged three-second bound; the published file remained and the only snapshot was the empty startup scan. Five mailbox repetitions and the full suite passed, but an additional combined repetition failed. The case remains enabled with diagnostics, and Phase 3 is not marked complete. This matrix does not establish reliable watcher-only delivery on that environment.

Each receiver drains one sorted filename snapshot at a time. A blocked delivery test proves overlapping watch and poll callbacks do not start another pass; a newly published file waits for the next drain. That is snapshot order, not producer order. Equal-timestamp suffixes can reverse calls, including bind-run and assignment publication. Forced filename collisions overwrite earlier payloads. Two receivers with separate in-flight sets can both inject the same file before either acknowledges it.

The listener marker is a PID liveness hint, not an exclusive ownership lock. A fixture-owned child exits before the dead-PID check; RPC send then chooses the modeled Herdr prompt fallback without publishing an envelope. No arbitrary PID or external pane is used.

Unreadable final JSON remains for a later drain; temporary `.tmp` files are ignored. Successfully parsed but invalid payloads are deleted. The real extension rejects unknown and stale peer panes. Lifecycle reports additionally require a matching saved run binding. An incoming unbound report is deleted, while an already recorded valid unbound report remains pending. The bound-report case counts one journal entry, one accepted lifecycle event, and envelope deletion separately from its custom-message injection.

A one-shot delivery rejection before `markInFlight` ends that pass and allows the next drain to retry. A modeled synchronous send throw after marking leaves that receiver stuck; stopping it and creating a fresh host permits file-only retry. These controlled-host faults do not establish natural Pi failure behavior. Hook unlink failure clears the in-flight ID first, so another hook cannot retry deletion; a later drain can use the recorded entry. Drain unlink failure ends the snapshot, and the next drain retries cleanup before continuing.

Pane sanitization keeps generated paths lexically inside a trusted root, and acknowledgement IDs must match the existing filename allowlist before hook unlink. The allowlist still accepts dotted names such as `..json` and `.hidden.json`. Neither check provides symlink-safe containment or unique pane encoding. Gap cases demonstrate pane aliases, reads through symlinked entries, and writes through symlinked ancestors. All targets stay inside the disposable fixture root. A direct local write that claims a known pane passes roster validation even with an unrelated sender ID. Roster membership is not writer authentication.

## Pi 0.84.4 compatibility

Development dependencies pin coding-agent and pi-ai to 0.84.4; consumer peer ranges remain unchanged. Every real-Pi case checks the imported project-local coding-agent, its resolved agent-core, and pi-ai metadata and prints their versions. Version drift fails rather than skips. The resource loader permits only the inline worker extension and rejects unexpected extensions, skills, prompts, themes, or context files. Only Herdr execution is replaced on the extension API. Pi owns hooks, flags, tools, custom-message delivery, and session appends.

The queue driver holds a synthetic assistant stream, publishes ordinary envelopes through the production transport, and invokes real `pi.sendMessage`. While the assistant is held, neither custom entry exists and both files remain. Releasing it lets steering consume before the earlier follow-up. Each custom `message_end` precedes its SessionManager append. `context` observes the appended entry and removes its file. The final `agent_settled` confirms both entries remain visible; reopening the actual session JSONL recovers their exact contents, details, and IDs. Text-only queue getters are not used as custom-queue evidence.

The storage cases distinguish a defined but absent session file from disabled persistence with no path. The first assistant flush writes buffered entries; later appends preserve earlier bytes. Replacing a fixture-owned session parent directory with a regular file produces `ENOTDIR` after Pi inserts the new entry into memory. Restoring the original directory before reopening proves the previous bytes survive unchanged and the failed entry is absent.

Three ordinary-message gap cases delete the only mailbox copy without a recoverable custom entry. The deferred-first-write case stops its assertions while the first assistant is held, before cleanup can append an aborted assistant. The disabled case remains without a path even after an assistant completes. The write-failed case keeps the directory fault active through Pi's real `agent_settled`, which acknowledges the memory entry; Pi reports the rejected send through its unchanged extension error bridge. These are loss-boundary observations in one process, not process-restart claims. Each gap row states the repair that would make its assertion obsolete.

The provider-error case has a narrower supported result. With persistent Pi 0.84.4 sessions, successful local writes, and retry and compaction disabled, failing the held synthetic assistant automatically consumes the queued follow-up once. Its `message_end` precedes append, `context` deletes the envelope, and a second assistant stream starts without another prompt or an intervening `agent_settled`. Reopening the actual session file recovers the exact custom entry at that boundary and after final settlement. Repeated drains do not reinject it. This does not extend to disabled or failed storage, other Pi versions, or external provider recovery.

Asynchronous send rejection remains a separate known gap. The fixture replaces only its individual session's `sendCustomMessage` method with a rejected promise, then restores it. Pi's unchanged void `pi.sendMessage` bridge reports one `send_message` extension error. No custom entry exists, and the readable envelope remains in flight through later drains even after restoration. This targeted fault injection does not claim a naturally occurring queue failure.

## Executable matrix

The test names include their category and stable ID. The mailbox test checks every cell against shared case metadata; each test file checks that its rows register executing bodies. Categories are exactly `Supported guarantee` and `Known contract gap`. Gap cases assert the current failure mechanism without skips, TODOs, or expected-failure markers.

| ID | Category | Test file | Test name | Envelope kind | Boundary | Persistence mode | Assertion | Assumptions | Gap removal condition |
|---|---|---|---|---|---|---|---|---|---|
| ordinary-publication-receipt | Supported guarantee | tests/extensions/herdr-worker-mailbox.test.ts | Supported guarantee: ordinary-publication-receipt | ordinary | Temporary write, rename, then RPC receipt | Controlled host, unconsumed mailbox | Hidden 0600 temporary JSON becomes the exact final envelope before the inbox receipt; no receiver consumption is claimed | Private root, live listener PID, valid team target, no filename collision | Not applicable |
| ordinary-queued-retention | Supported guarantee | tests/extensions/herdr-worker-mailbox.test.ts | Supported guarantee: ordinary-queued-retention | ordinary | Pi handoff before a matching custom entry | Controlled host, queued only | Repeated drains and acknowledgement hooks leave the file with one injection and no custom entry in memory or reopened storage | One receiver instance, known peer pane, no matching custom entry | Not applicable |
| ordinary-context-ack | Supported guarantee | tests/extensions/herdr-worker-mailbox.test.ts | Supported guarantee: ordinary-context-ack | ordinary | Matching custom entry then context | Controlled host, explicit JSONL write before hook | One injection, exact custom entry reopened from JSONL, then context deletes the file; a drain alone still skips the in-flight file | One receiver instance, known peer pane, successful explicit test-file write | Not applicable |
| ordinary-agent-settled-ack | Supported guarantee | tests/extensions/herdr-worker-mailbox.test.ts | Supported guarantee: ordinary-agent-settled-ack | ordinary | Matching custom entry then agent_settled | Controlled host, explicit JSONL write before hook | One injection, exact custom entry reopened from JSONL, then agent_settled deletes the file; a drain alone still skips the in-flight file | One receiver instance, known peer pane, successful explicit test-file write | Not applicable |
| ordinary-fresh-receiver-ack | Supported guarantee | tests/extensions/herdr-worker-mailbox.test.ts | Supported guarantee: ordinary-fresh-receiver-ack | ordinary | Fresh receiver with a matching reopened custom entry | Controlled host, file-only reconstruction | The replacement cleans the retained filename without reading its payload or injecting another custom message | Same pane, root and test session file, matching filename, previous receiver stopped | Not applicable |
| ordinary-headless-listener-ownership | Supported guarantee | tests/extensions/herdr-worker-mailbox.test.ts | Supported guarantee: ordinary-headless-listener-ownership | ordinary | Headless shutdown beside an interactive listener | Controlled TUI and headless hosts | Headless shutdown preserves the other listener marker and callbacks; owner teardown removes them, all subscriptions and the private root | Headless host never started listening; interactive owner remains alive until teardown | Not applicable |
| snapshot-order-overlap | Supported guarantee | tests/extensions/herdr-worker-mailbox.test.ts | Supported guarantee: snapshot-order-overlap | ordinary | Blocked first delivery with overlapping watch and poll triggers | Controlled host, explicit queue and memory | One sorted snapshot delivers a, b, then the later timestamp once each; overlapping triggers do not rescan and a late file waits for the next explicit drain | One receiver, known peer, private trusted root | Not applicable |
| real-watch-delivery | Supported guarantee | tests/extensions/herdr-worker-mailbox.test.ts | Supported guarantee: real-watch-delivery | ordinary | Publish after listener startup with poll callback disabled | Controlled host, real fs.watch | A bounded real fs.watch notification reaches the extension and queues one custom message; the envelope remains | Private root, live real watcher, no poll invocation | Not applicable |
| watch-failure-poll-fallback | Supported guarantee | tests/extensions/herdr-worker-mailbox.test.ts | Supported guarantee: watch-failure-poll-fallback | ordinary | Watch creation throws before controlled polling | Controlled host, explicit queue and memory | Listener remains started; explicit poll callback delivers the published file once and shutdown removes the marker | One receiver, known peer, private trusted root | Not applicable |
| dead-listener-prompt-fallback | Supported guarantee | tests/extensions/herdr-worker-mailbox.test.ts | Supported guarantee: dead-listener-prompt-fallback | ordinary | Listener marker names an exited fixture-owned child | Controlled Herdr prompt and real PID liveness | Current PID is live, dead child PID is not; real RPC send returns prompt fallback without a mailbox publication | Owned child has exited, known peer, private marker, no arbitrary PID probes | Not applicable |
| partial-json-and-hidden-temp | Supported guarantee | tests/extensions/herdr-worker-mailbox.test.ts | Supported guarantee: partial-json-and-hidden-temp | ordinary | Incomplete final JSON is later completed | Controlled host, explicit queue and memory | Partial final JSON remains unreadable across drains; hidden temporary JSON is ignored; completing the final file injects once | One receiver, known peer, private trusted root | Not applicable |
| parsed-invalid-cleanup | Supported guarantee | tests/extensions/herdr-worker-mailbox.test.ts | Supported guarantee: parsed-invalid-cleanup | ordinary | Successful parse followed by invalid or falsy payload | Controlled host, explicit queue and memory | Invalid shapes and falsy JSON are deleted without custom messages; no parse failure is treated as invalid work | One receiver, known peer, private trusted root | Not applicable |
| ordinary-priority-routing | Supported guarantee | tests/extensions/herdr-worker-mailbox.test.ts | Supported guarantee: ordinary-priority-routing | ordinary | Real extension handoff to modeled Pi queue | Controlled host, explicit queue and memory | Priority true maps to steer and false to followUp; both set display and triggerTurn and retain their files | One receiver, known peer, private trusted root | Not applicable |
| lifecycle-follow-up-routing | Supported guarantee | tests/extensions/herdr-worker-mailbox.test.ts | Supported guarantee: lifecycle-follow-up-routing | lifecycle | Bound report handoff, memory append, then context | Controlled host, explicit report write and real journal append | Valid report uses followUp, remains while queued, then produces one journal event and cleanup at context | Saved valid run metadata selects worker pane and contract 2; real lifecycle acceptor | Not applicable |
| ordinary-peer-rejection | Supported guarantee | tests/extensions/herdr-worker-mailbox.test.ts | Supported guarantee: ordinary-peer-rejection | ordinary | Unknown pane and formerly known pane after roster movement | Controlled host, explicit queue and memory | Both unauthorized envelopes are deleted without injection; the currently known pane still delivers | One receiver, known peer, private trusted root | Not applicable |
| lifecycle-peer-rejection | Supported guarantee | tests/extensions/herdr-worker-mailbox.test.ts | Supported guarantee: lifecycle-peer-rejection | lifecycle | Unknown or stale live peer and mismatched bound pane | Controlled host, explicit queue and memory | Valid reports from unknown, old, and live-but-not-bound panes are deleted without injection or accepted events | Saved contract-2 binding, controlled peer movement, private root | Not applicable |
| lifecycle-incoming-unbound | Supported guarantee | tests/extensions/herdr-worker-mailbox.test.ts | Supported guarantee: lifecycle-incoming-unbound | lifecycle | Known peer report arrives without saved run binding | Controlled host, explicit queue and memory | Unbound incoming report is deleted before custom-message injection or lifecycle acceptance | One receiver, known peer, private trusted root | Not applicable |
| lifecycle-recorded-unbound-pending | Supported guarantee | tests/extensions/herdr-worker-mailbox.test.ts | Supported guarantee: lifecycle-recorded-unbound-pending | lifecycle | Already recorded valid report without run binding | Controlled host, recorded memory entry | Matching recorded report remains pending through hooks and drain with no injection, deletion, or lifecycle event | Explicit recorded report and marked filename, no saved binding | Not applicable |
| delivery-reject-before-mark | Supported guarantee | tests/extensions/herdr-worker-mailbox.test.ts | Supported guarantee: delivery-reject-before-mark | ordinary | Delivery callback rejects before markInFlight | Controlled callback fault model | File and remaining snapshot files survive the ended pass; later explicit drain retries and injects each once | One-shot callback fault before the real extension handler, one receiver | Not applicable |
| send-throw-after-mark | Known contract gap | tests/extensions/herdr-worker-mailbox.test.ts | Known contract gap: send-throw-after-mark | ordinary | Synchronous pi.sendMessage throws after markInFlight | Controlled synchronous send failure, file-only host replacement | File remains readable but same-instance drains and hooks cannot retry without an entry; a fresh host retries from the file | Model-only synchronous void-API fault, old host stopped before replacement, no retained queue | Failed handoffs clear or reconcile in-flight state so the same receiver can retry |
| hook-unlink-retry | Supported guarantee | tests/extensions/herdr-worker-mailbox.test.ts | Supported guarantee: hook-unlink-retry | ordinary | Hook unlink throws after clearing in-flight | Controlled host, file-backed acknowledgement | A second hook does not retry unlink; a later drain uses the written session entry to remove the file without another injection | One-shot local unlink fault; report already written and reopened | Not applicable |
| drain-unlink-retry | Supported guarantee | tests/extensions/herdr-worker-mailbox.test.ts | Supported guarantee: drain-unlink-retry | ordinary | Acknowledged-entry drain unlink throws | Controlled host, written session entries | Outer catch ends the pass before remaining files; next drain retries cleanup without another custom send | Matching entries, no in-flight IDs, one-shot unlink fault | Not applicable |
| lexical-pane-containment | Supported guarantee | tests/extensions/herdr-worker-mailbox.test.ts | Supported guarantee: lexical-pane-containment | ordinary | Publication with traversal, absolute-looking, separator and control-character pane IDs | Real filesystem publication | Generated final and temporary paths stay lexically inside the mailbox, contain exact envelopes, and leave outside sentinels unchanged | Private trusted root without symlinks; extension-style numeric timestamps | Not applicable |
| acknowledgement-allowlist | Supported guarantee | tests/extensions/herdr-worker-mailbox.test.ts | Supported guarantee: acknowledgement-allowlist | ordinary | Malformed acknowledgement IDs deliberately marked in flight | Controlled host, memory acknowledgement | Hooks reject traversal, absolute, separator, control-character and non-json IDs before unlink; accepted dotted names are still removed | Private trusted root, matching recorded entries and explicit markInFlight | Not applicable |
| suffix-reverses-producer-order | Known contract gap | tests/extensions/herdr-worker-mailbox.test.ts | Known contract gap: suffix-reverses-producer-order | ordinary | Equal timestamp writes with reverse-sorted suffixes | Controlled host, queued only | Second producer call is injected first because the snapshot sorts final filenames | Fixed valid filenames, same timestamp, one receiver | Publication adds and enforces producer sequencing instead of random suffix order |
| filename-collision-overwrite | Known contract gap | tests/extensions/herdr-worker-mailbox.test.ts | Known contract gap: filename-collision-overwrite | ordinary | Two writes use the same final filename | Controlled host, queued only | Only the second envelope survives and is injected; the first payload is overwritten | Forced final-name collision before any drain | Exclusive publication detects collisions and preserves both messages |
| two-receivers-duplicate | Known contract gap | tests/extensions/herdr-worker-mailbox.test.ts | Known contract gap: two-receivers-duplicate | ordinary | Two transports reach delivery before either acknowledges | Controlled host, queued only | Both instances inject the same filename into separate queues; one later acknowledgement deletes the shared file | Two hosts share one inbox, independent in-flight sets, explicit delivery barrier | Receiver ownership or inter-process coordination grants exclusive delivery |
| bind-assignment-reordered | Known contract gap | tests/extensions/herdr-worker-mailbox.test.ts | Known contract gap: bind-assignment-reordered | control | Same timestamp bind-run publication precedes assignment with earlier suffix | Controlled host, queued only | Assignment reaches the worker before active binding is set, then bind-run persists the binding | Known peer, valid binding, fixed reverse-sorted filenames, no readiness listener | Control and assignment sequencing enforces binding before assignment handoff |
| pane-sanitization-alias | Known contract gap | tests/extensions/herdr-worker-mailbox.test.ts | Known contract gap: pane-sanitization-alias | ordinary | Distinct pane IDs sanitize to the same directory | Real filesystem publication | Both claimed recipient IDs publish into the same inbox | Private root; a/b and a_b are distinct inputs | Pane directory encoding is injective or collisions are rejected |
| symlink-entry-read | Known contract gap | tests/extensions/herdr-worker-mailbox.test.ts | Known contract gap: symlink-entry-read | ordinary | Final inbox entry links to a sentinel outside mailbox subtree | Controlled host, queued only | Drain reads and injects the outside sentinel payload; acknowledgement removes only the symlink | All symlink targets stay inside the disposable test root | No-follow file opening rejects symlinked inbox entries |
| symlink-ancestor-write | Known contract gap | tests/extensions/herdr-worker-mailbox.test.ts | Known contract gap: symlink-ancestor-write | ordinary | Recipient directory is a symlink to outside mailbox subtree | Real filesystem publication | Publication follows the ancestor and writes the final envelope outside the lexical mailbox root | Symlink and redirected files remain inside disposable test root | Directory ownership and no-follow containment reject redirected ancestors |
| known-pane-impersonation | Known contract gap | tests/extensions/herdr-worker-mailbox.test.ts | Known contract gap: known-pane-impersonation | ordinary | Local writer claims a known pane with unrelated sender ID | Controlled host, queued only | Roster validation permits the forged message and preserves the claimed sender in details | Fixture writes the file directly; claimed pane is currently known | Receiver authenticates the writer rather than only checking the claimed pane |
| pi-ordinary-queue-consumption | Supported guarantee | tests/extensions/herdr-worker-pi-compat.test.ts | Supported guarantee: pi-ordinary-queue-consumption | ordinary | Held assistant, real steering and follow-up consumption, context and agent_settled | Pi 0.84.4 persisted session, first assistant flush then queued appends | Both files remain while queued; steering consumes before earlier follow-up; message_end precedes memory append; context deletes only consumed files and reopened JSONL contains exact payloads and IDs | Project-local Pi coding-agent and resolved agent-core 0.84.4, isolated paths, synthetic assistant streams, no provider requests, one receiver and a known peer pane | Not applicable |
| pi-custom-first-flush-and-append | Supported guarantee | tests/extensions/herdr-worker-pi-compat.test.ts | Supported guarantee: pi-custom-first-flush-and-append | ordinary | Direct custom append before first assistant, first flush, then later custom append | Pi 0.84.4 persisted SessionManager with deferred first write | Persistence is enabled with a defined but absent file; first assistant flush recovers the exact buffered custom entry and a later append recovers independently | Project-local Pi coding-agent and resolved agent-core 0.84.4, isolated paths, synthetic assistant streams, no provider requests, public SessionManager append APIs and successful local writes | Not applicable |
| pi-custom-append-error-memory | Known contract gap | tests/extensions/herdr-worker-pi-compat.test.ts | Known contract gap: pi-custom-append-error-memory | ordinary | Real appendCustomMessageEntry filesystem exception after memory insertion | Pi 0.84.4 flushed session, parent directory replaced by a regular file | ENOTDIR leaves the new custom entry and leaf in memory; restoring the directory recovers unchanged prior bytes and no failed entry | Project-local Pi coding-agent and resolved agent-core 0.84.4, isolated paths, synthetic assistant streams, no provider requests, fixture-owned parent restored before reopen, no mocked persistence methods | Pi rolls back memory insertion on append failure or separates committed entries from its memory view |
| pi-ordinary-ack-before-first-write | Known contract gap | tests/extensions/herdr-worker-pi-compat.test.ts | Known contract gap: pi-ordinary-ack-before-first-write | ordinary | Idle-triggered custom message reaches context before the first assistant completes | Pi 0.84.4 persistence enabled, first write deferred | Context sees the custom entry and deletes the only mailbox copy while the session path is still absent and no file entry is recoverable | Project-local Pi coding-agent and resolved agent-core 0.84.4, isolated paths, synthetic assistant streams, no provider requests, brand-new persisted session, known peer, first assistant held at the loss boundary | Mailbox acknowledgement requires a recoverable session write before deleting the envelope |
| pi-ordinary-ack-persistence-disabled | Known contract gap | tests/extensions/herdr-worker-pi-compat.test.ts | Known contract gap: pi-ordinary-ack-persistence-disabled | ordinary | Ordinary context acknowledgement with SessionManager.inMemory | Pi 0.84.4 persistence disabled | The custom entry permits deletion but no session path or recovered entry exists, even after the assistant completes | Project-local Pi coding-agent and resolved agent-core 0.84.4, isolated paths, synthetic assistant streams, no provider requests, known peer, inMemory has no restored-entry input | Mailbox retains envelopes or refuses acknowledgement when session persistence is disabled |
| pi-ordinary-ack-write-failed | Known contract gap | tests/extensions/herdr-worker-pi-compat.test.ts | Known contract gap: pi-ordinary-ack-write-failed | ordinary | Idle-triggered custom append fails, then real agent_settled acknowledges memory | Pi 0.84.4 flushed session, parent directory replaced by a regular file | The real append error leaves a memory entry; agent_settled deletes the mailbox file, and restored prior session bytes contain no recovered custom entry | Project-local Pi coding-agent and resolved agent-core 0.84.4, isolated paths, synthetic assistant streams, no provider requests, known peer, fixture-owned directory fault remains active through acknowledgement | Failed session writes cannot supply acknowledged entries or mailbox deletion waits for recoverable storage |
| pi-send-rejection-in-flight | Known contract gap | tests/extensions/herdr-worker-pi-compat.test.ts | Known contract gap: pi-send-rejection-in-flight | ordinary | Individual session.sendCustomMessage returns an injected rejected promise | Pi 0.84.4 targeted asynchronous send fault, unchanged void extension bridge | Pi reports one send_message error; no custom entry exists; the readable envelope stays in flight and later drains do not retry even after method restoration | Project-local Pi coding-agent and resolved agent-core 0.84.4, isolated paths, synthetic assistant streams, no provider requests, injected session method restored at teardown, not a naturally occurring queue failure | Pi handoff failures can reconcile the mailbox in-flight state and permit same-instance retry |
| pi-provider-failure-queued | Supported guarantee | tests/extensions/herdr-worker-pi-compat.test.ts | Supported guarantee: pi-provider-failure-queued | ordinary | Active synthetic assistant fails while a custom follow-up is queued | Pi 0.84.4 persistent session, real queue, synthetic provider error | Automatic continuation consumes the queued message once, context deletes its envelope, and the next assistant starts without another prompt; actual session-file reopen recovers the exact custom entry before and after settlement with no duplicate delivery | Project-local Pi coding-agent and resolved agent-core 0.84.4, isolated paths, synthetic assistant streams, no provider requests, persistence enabled with successful local writes, one receiver and known peer, no retry or compaction; excludes disabled or failed storage and other Pi versions | Not applicable |
