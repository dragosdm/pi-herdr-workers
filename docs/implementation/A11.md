# A11 implementation evidence

Date: 2026-09-20. Specification: [A11](../specs/A11.md), ordered by the [spec index](../specs/README.md), specs PR [#11](https://github.com/dragosdm/pi-herdr-workers/pull/11).

Implementation base and PR base: `fix/audit-a03` at `131912d1ca071dfe156c8bc018b4dc04a3a786a0`. Branch: `fix/audit-a11`. Worktree: `/Users/dragos/workspace/internal/pi-herdr-workers-fix-a11`.

## Cause and baseline

The due `fireTimes` path skipped awaiting dynamic controllers and applied the work filter before checking expiry. Moving the existing expiry block ahead of both checks makes lifetime enforcement independent of work eligibility. The initial due-time and active/specially-managed guards are unchanged. Retirement still uses `retireExpired`, including its unconditional `continue` when context permission is denied.

Read the spec and index fully, A03 implementation evidence, scheduler, relevant store/reducer/runtime/tool paths, persistence implementation, existing audit and A03 tests. The historical A11 audit describes an advanced-clock reproduction, not seven days of live observation. The retained JSON's `dynamic-resume-update`, `dynamic-double-update`, and `dynamic-pause-state` records concern update behavior. Its dynamic reload record is paused and not awaiting an update. Those records are not live A11 expiry evidence, and the audit files remain unchanged.

Before changing production code, added only the primary positive regression and ran:

```sh
node --import tsx --test tests/extensions/a11-awaiting-update-expiry.test.ts
```

Exit 1; **0 passed, 1 failed, 0 skipped, 0 todos**, 938.438 ms. At the exact deadline, `store.get(id)` returned the active awaiting controller rather than `undefined`. Its checkpoint and original deadline were present in the failed assertion. The same primary test passes after the reorder, with no work callback and one `deleted` expiry callback. The file was then expanded to 35 tests.

An intermediate matrix run had two test-fixture failures because error-tone tool results throw rather than return an error object. The assertions now use `assert.rejects` against that existing contract. No production API was changed to satisfy those tests.

## Changed files and compatibility

- `loop/scheduler.ts`: move one existing expiry block and add one explanatory comment. No helper, store, reducer, runtime, parser, or trigger API change.
- `tests/extensions/a11-awaiting-update-expiry.test.ts`: 35 dedicated tests using real stores and schedulers, callback observations, test-local `Date.now` mocks, snapshots, actual tool updates, and an owned temporary file-store directory.
- `tests/extensions/audit-loop-monitor.test.ts`: rename and invert only A11's exact characterization. Keep its deadline-plus-one input and every unrelated assertion.
- `README.md`: state that waiting does not suspend expiry, recovery preserves deadlines, and retirement does not cancel executing work. Preserve A03's quarantine documentation.
- `docs/implementation/A11.md`: this evidence and acceptance checklist.

The intentional behavior change also covers overdue non-waiting entries behind rejecting work filters. Unexpired filtering is unchanged. Ordinary dynamic expiry deletes; task-backlog expiry retains its existing controller-limit pause. Fire-cap retirement remains separate. Waiting checkpoints are untouched before expiry. No deadline renewal, new acknowledgement token, resume policy, paused-controller policy, schema, snapshot version, tool input, event payload, package, lockfile, or dependency change was made.

A03's preflight, resolver error classification and isolated recovery remain inherited without edits. Shared-file changes were limited to the specified README paragraphs, the A11 audit block, and scheduler pump ordering; no conflict resolution or shared-fixture adaptation was needed. A08/A09 are not on this base. Combined token/pause/expiry acceptance remains the coordinator's integration responsibility.

## Regression coverage

| Spec matrix area | Dedicated checks |
| --- | --- |
| Primary and inclusive boundary | Exact deadline and +1 delete without work; -1 compares the full snapshot, including checkpoint, iteration, fire count and deadline |
| Real iteration and wake forms | `store.fire` plus `updateDynamic` marks awaiting and clears next wake; no repeated dispatch; omitted and explicit earlier wakes expire |
| Both timer maps and filtering | Wake at/beyond expiry arms expiry-only; deadline-minus-one survives; overdue waiting/non-waiting entries bypass the filter without evaluating it; unexpired rejection still blocks work |
| Permission and idempotence | Both maps with waiting/non-waiting entries retain state when permission is denied and retire on retry; repeated pumps and stop/start produce one saved deletion and one callback |
| Cancellation and restoration | Explicit deletion/pause is not reported as expiry; stopped scheduler does nothing; restored waiting checkpoint survives unchanged until its saved deadline; recovery's `expireEntries` retires overdue state before scheduler start |
| Persistence and errors | Callback snapshot cannot revive deletion; a new file-store instance sees deletion; failed memory append rolls back without callback and retries once; callback failure cannot permit more work; already-removed store transition sends no false expiry notification |
| Update ordering | Real `LoopUpdate` before expiry preserves deadline; at/after deadline it rejects before pump; store continue/resume reject without mutation; after retirement the tool rejects the missing ID without recreation. Wall time and pump time are both controlled |
| Existing dispositions and scheduling | Dynamic task-backlog persists one controller-limit pause; ordinary cron/event expiry deletes; valid dynamic work fires; fire cap pauses without an expiry callback; mixed maps process every expired entry and valid due work |

## Verification

All commands ran as bounded foreground processes in this worktree. No skips or todos occurred.

| Command | Actual result |
| --- | --- |
| Pre-fix primary command above | Exit 1 as required; 0 passed, 1 failed; 938.438 ms |
| `node --import tsx --test tests/extensions/a11-awaiting-update-expiry.test.ts tests/extensions/audit-loop-monitor.test.ts` | Exit 0; 50 passed, 0 failed, 0 skipped, 0 todos; 1936.026 ms. Includes 35 dedicated A11 and 15 shared audit checks |
| `npm run typecheck` | Exit 0; `tsc --noEmit`, no diagnostics |
| `npm test` | Exit 0; 619 passed, 0 failed, 0 cancelled, 0 skipped, 0 todos; 93679.772 ms |
| `git diff --check` | Exit 0; no whitespace errors |

The full suite includes inherited A03 coverage and unrelated passing defect characterizations. Their success is not proof that those other findings are fixed. No raw logs or session content are committed.

No live check was run. A11 does not require one; deterministic clock tests cover its failing branch. No panes, model turns, production clocks or user controllers were touched. Retirement is promised on an eligible pump or recovery, not within a hard real-time bound. This does not establish external notification exactly-once delivery, cancellation of work already handed off, power-loss durability, or exhaustive regression freedom.

## Definition-of-done checklist

Each item follows the specification's checklist order.

1. [x] Due fire-map expiry precedes waiting and filter checks; active/special guards and `canExpire` remain intact.
2. [x] Ordinary awaiting dynamic entries retire at and after the exact deadline with no additional work callback.
3. [x] Full snapshot comparisons prove unexpired waiting checkpoints remain unchanged, including after restoration.
4. [x] Existing store helper deletes or pauses, persists once, removes registrations, and emits one scheduler expiry callback across repeated pumps.
5. [x] Permission deferral and failed memory append preserve state/registration without overdue dispatch or false acknowledgement; bounded retry tests pass.
6. [x] All matrix areas above have dedicated coverage; the unchanged primary positive test failed before production edits.
7. [x] Only the exact A11 known-gap assertion was converted; unrelated audit checks remain.
8. [x] Targeted command passes all 50 cases without skips or todos.
9. [x] Typecheck passes without diagnostics.
10. [x] Full suite passes all 619 tests on the exact inherited A03 base.
11. [x] Whitespace validation passes.
12. [x] README describes waiting expiry, recovery and non-cancellation. Historical audit files are untouched.
13. [x] Reviewed diff contains only the five scoped files above. No unrelated production, schema, package or other-finding behavior change.
14. [ ] Commit/push/PR delivery is recorded by the branch and worker completion report, with this evidence in the PR. Independent coordinator PR review remains pending, so the combined delivery-and-review item is not marked accepted. No merge is authorized.

Automated implementation verification is complete. Independent review and combined A08/A09/A11 integration verification are still pending; this report does not claim independent acceptance.
