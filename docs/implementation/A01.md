# A01 implementation evidence

Date: 20 September 2026. Automated checks pass; required live acceptance is pending.

- Specification: [A01](../specs/A01.md), ordered by the [spec index](../specs/README.md), specs PR [#11](https://github.com/dragosdm/pi-herdr-workers/pull/11).
- Worktree: `/Users/dragos/workspace/internal/pi-herdr-workers-fix-a01`.
- Branch: `fix/audit-a01`, based directly on `specs/audit-findings` at `565497884b1ecaf851e7452bba40d4648eab6f10`.
- Implementation commit: [`5faabd248e5bcba950ce42e61cb0572c4aeb65ce`](https://github.com/dragosdm/pi-herdr-workers/commit/5faabd248e5bcba950ce42e61cb0572c4aeb65ce). The following documentation commit records this evidence and the audit resolution note.
- Runtime used for automated checks: Node `v24.15.0`.

## Baseline and positive regression

On the unchanged base, this command passed the original defect characterization:

```text
node --import tsx --test --test-name-pattern='busy shell startup' tests/extensions/audit-loop-monitor.test.ts
1 test, 1 pass, 0 failures, 0 skips; exit 0
```

Before changing production code, the new `monitor-startup.test.ts` contained the primary positive regression, using a startup helper followed by a shell and ownership evidence:

```text
node --import tsx --test tests/extensions/monitor-startup.test.ts
1 test, 0 passes, 1 failure, 0 skips; exit 1
Expected pane run count 1; actual 0 (0 !== 1).
```

After implementation, the same helper-to-shell contract passes for splits, fresh tab roots and claimed idle roots. The dedicated file now contains 84 passing cases. The owned historical A01 characterization now requires one run, submitted launch evidence/action, no signal and no reuse. Other audit characterizations, including A02 tail parsing, are unchanged.

## Verification results

All commands ran as bounded foreground processes in this worktree, without monitor tools or sleep polling. No package or lock files changed. The following results are from the final implementation tree; the documentation-only follow-up does not change code or test expectations.

| Command | Actual result |
| --- | --- |
| `node --import tsx --test tests/extensions/monitor-startup.test.ts` | Exit 0; 84 tests, 84 passed, 0 failed/cancelled/skipped/todo |
| `node --import tsx --test tests/extensions/audit-loop-monitor.test.ts` | Exit 0; 15 tests, 15 passed, 0 failed/cancelled/skipped/todo |
| `npm run typecheck` | Exit 0; `tsc --noEmit`, no diagnostics |
| `npm test` | Exit 0; 637 tests, 637 passed, 0 failed/cancelled/skipped/todo |
| `git diff --check` and staged whitespace check | Exit 0; no whitespace errors on the final tree |

The first staged whitespace check found one extra EOF blank line in the new regression file. It was removed before the implementation commit; the repeated staged check passed. An earlier typecheck caught an inferred `string` return action; the explicit `MonitorCreateResult` serialization type corrected it before final verification. No failing assertions were removed to obtain the green results.

## Changed files and compatibility

| File | Reason |
| --- | --- |
| `loop/runtime/herdr-monitor.ts` | Track launch evidence; retain handles before rename; distinguish fresh roots; verify readiness and ownership under a monotonic five-second budget; combine caller/disposal cancellation; persist uncertainty before one submission; protect list/stop and recovery paths. |
| `loop/tools/monitor-tools.ts` | Render this call's submission or attachment action, pending list rows and actionable thrown errors; refresh the widget on failure. Tool names and parameter schemas are unchanged. |
| `tests/extensions/monitor-startup.test.ts` | Dedicated scripted manager/adapter regressions, deterministic deadlines, snapshot envelopes and cancellation controls. |
| `tests/extensions/audit-loop-monitor.test.ts` | Convert only A01 from broken-behavior characterization to a positive regression. |
| `README.md` | Explain bounded readiness, retained panes, observational status and inspection before retry after uncertainty. |
| `docs/audits/2026-09-20-live-extension.md` | Add a dated A01 resolution note; preserve historical observations and evidence JSON. |
| `docs/implementation/A01.md` | Record implementation and acceptance evidence. |

`launchState` is optional in the existing `herdr-monitors.snapshot.v1` envelope. Legacy entries retain no invented evidence, invalid states become uncertain, and process status resets to unknown on restore. The transient `createAction` is never stored. A registered-extension test exercises the v1 envelope, persistence and exclusion of other session/workspace scopes, so `loop/index.ts` needs no change.

The command/cwd hash, quoting, shell allowlist, case-sensitive Monitor tab, current workspace, down-split/no-focus flags, monotonic IDs and 25 running/unknown cap are retained. Busy labeled panes are observational attachments with no input or rename. Explicit idle reuse can submit again; no cross-call exactly-once promise is made. Pending stop returns false immediately, including during an active readiness wait, so it cannot queue an interrupt that later hits the submitted command.

Readiness checks use at most 100 ms between observations, a 5,000 ms monotonic deadline and the remaining budget for process-info and ownership CLI timeouts. Other CLI calls retain 15,000 ms. The deadline abort also covers a hung CLI promise. Pre-run persistence failure prevents input; post-attempt errors retain uncertainty and include both submission and failure-append causes when needed.

## Coverage and definition of done

- [x] Scope is confined to A01, its recovery/result behavior, tests and documentation. No package, lock, specification, loop or worker production changes.
- [x] Fresh split/root startup waits for verified shell readiness and submits once, or fails unsubmitted within the readiness budget. Immediate readiness, 21 malformed data shapes, PID/name mismatches, delayed responses, hung probes, CLI/JSON failures and shared ownership budget are tested.
- [x] Pending create, retry, restored create and list cannot report an attached/running requested command. Pending stop sends no Ctrl-C, including during startup.
- [x] Busy existing monitors still attach without input, rename, signals or pane closure. Explicit idle reuse submits once; concurrent creates cover both long-lived commands and legitimate reruns after completion.
- [x] The handle is saved before rename/run. Snapshot round trips preserve launch evidence, normalize invalid states and keep legacy snapshots readable. Restore and cross-session/scope inspection launch nothing.
- [x] Caller cancellation and disposal prevent late launches before acquisition, during wait/probe and immediately before run. Queued cancellation returns promptly and the queue remains usable. Deadline timers are cleared on all test paths; the default abortable wait is exercised. Disposal suppresses later persistence and never closes panes.
- [x] Timeout/cancellation after attempted submission is uncertain and does not retry. Failed acknowledgement and failure-state appends are covered; restore does not replay input.
- [x] Original A01 characterization is now a positive regression. All 14 other audit cases retain their assertions.
- [x] Dedicated regression matrix passes, including acquisition, ownership loss, stale labels, deadlines before timer delivery, acquisition outside the readiness budget, concurrency, recovery, list races, tool wording/tone and unchanged schemas.
- [x] Targeted tests, typecheck, the full suite and whitespace checks pass with the counts above. Source/test diff reviewed before commit.
- [x] README and dated audit resolution note describe submission semantics and uncertainty without changing historical evidence.
- [ ] Required bounded disposable live verification. Pending supervisor coordination; no live verification is claimed by this worker. No valuable user panes or live monitor tabs were touched. Optional unrelated live checks were omitted.
- [x] Focused implementation commit identifies A01; the implementation PR targets the exact specs base, links A01 and specs PR #11, records automated evidence and discloses the pending live gate. PR URL and final documentation commit SHA are reported in the worker completion result.

## Required supervisor live check

Use the exact safe driver procedure in [A01's bounded live verification](../specs/A01.md#bounded-live-verification), from this implementation checkout, in an approved disposable workspace with no valuable Monitor tab. Record installed Herdr version, workspace/pane IDs and pre-existing panes.

1. First create only: append `A01-first` to a fresh temporary canary. Expect `command submitted` and exactly one line within ten seconds. Do not retry to make the check pass.
2. Create one disposable Node interval script that appends one `start` line. Repeat the identical create only after it is busy. Expect attachment with no submission and exactly one start line.
3. Stop only the returned monitor ID, verify its foreground returned to the shell, shut down the driver, and close only recorded test-created panes/tabs. Save sanitized responses, canary bytes, CLI version and cleanup confirmation.

A02's independent tail failure remains, so tails are not the startup oracle. Unit tests do not replace this required live gate. Shell process inspection does not prove application readiness or prevent another actor from changing the pane before input. Session append durability is unchanged; neither power-loss safety nor exactly-once external effects are claimed. Independent coordinator review and live acceptance remain required; no PR merge is authorized.
