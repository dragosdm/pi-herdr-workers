# A07 implementation evidence

Implemented against `fix/audit-a05` at `6341f9eccfac8a8dd002df013f6dcb1957558487`, retaining A03 and A05. Branch: `fix/audit-a07`. Worktree: `/Users/dragos/workspace/internal/pi-herdr-workers-fix-a07`.

Scope follows [A07](../specs/A07.md) and the ordering in the [spec index](../specs/README.md), approved in [specs PR #11](https://github.com/dragosdm/pi-herdr-workers/pull/11). The implementation commit and review PR are recorded in the PR and worker completion report; this file travels with that commit.

## Before and after

Before changing production code, the dedicated positive tests ran on the exact A05 base:

```sh
node --import tsx --test --test-name-pattern='complete cron reaches LoopCreate' tests/extensions/loop-hybrid-trigger.test.ts
```

Exit 1: 4 tests, 0 passed, 4 failed, no skips. The expressions `*/5 * * * *`, `0 9 * * 1-5`, `0,30 9-17 * * 1-5`, and `0-30/5 * * * *` failed because `parseInterval()` received only `*/5`, `0`, `0,30`, or `0-30/5`. This reproduces the truncation in the historical audit without treating its rejection characterization as correctness.

After the fix, all four requests succeed, preserving the full schedule and exact source. Creation and listing show both values. Tests observe a scheduled next fire and one event listener.

## Changes and compatibility

| File | Reason |
| --- | --- |
| `loop/hybrid-parse.ts` | Pure, anchored clause extraction and whitespace normalization. No store, time, event or filesystem access; no cron validation fork. |
| `loop/tools/loop-tools.ts` | Replace only hybrid extraction, call shared `parseInterval()` once, retain its result, and add hybrid context to schedule errors. Expand the existing trigger description without changing schema fields or types. |
| `tests/extensions/loop-hybrid-trigger.test.ts` | 70 positive and negative regressions using real tools/store/scheduler/triggers, controlled Date and timeout mocks, and TypeBox schema checks. |
| `tests/extensions/audit-loop-monitor.test.ts` | Convert only the A07 rejection test to successful full-cron creation with timer/listener assertions and cleanup. |
| `README.md` | Copyable tool examples, complete grammar, compatibility defaults and intentional rejection of formerly ignored junk. |
| `docs/implementation/A07.md` | Reproduction, verification and acceptance checklist. |

Both label orders, colonless labels, compact colon spelling, whitespace-separated shorthand, bare schedules and cron-only schedules remain supported. Missing event clauses default to `tool_execution_start` only for explicit hybrid inputs; empty clauses reject. Event names preserve case, colons, literal single-token quotes and backslashes. Invalid schedule syntax remains the shared parser's responsibility. Error messages begin `Invalid hybrid trigger:`, retain shared-parser diagnostics and include the preferred example.

Parser failures leave the complete snapshot, next ID, persistence callback count, registration count, journal calls and widget updates unchanged. They create no listener or next fire. The syntactically valid impossible hybrid `0 0 31 2 *` still follows A03 preflight, with no mutation. A05's unsupported `0m` and `2d` remain errors, including inside hybrids. Ordinary cron errors are not rewrapped.

No changes to trigger storage, journal versions/scopes, IDs, the seven-day lifetime, 25-controller/fire-cap defaults, read-only handling, runtime debounce, pause/delete, inference, scheduler semantics or dependency files. The fixtures persist fire counts using the actual callback origin. Both cross-source cap orderings dispatch at most once. Pause/delete before a wake, pending-debounce pause/delete, and shutdown suppress later dispatch. An uncanceled pending debounce still fires. Old structured shorthand records with filters and new full-cron records round-trip through snapshots; repeated starts keep one listener/next fire per active controller. Paused records stay inactive. The old runtime stops before restoration; restoration dispatches no catch-up wake. Node resets context-owned timer mocks after each test, and each fixture stops its trigger system in cleanup.

## Verification

All commands ran in this worktree with bounded foreground execution. Final automated results:

| Command | Result |
| --- | --- |
| `node --import tsx --test tests/extensions/loop-hybrid-trigger.test.ts` | Exit 0; 70 passed, 0 failed/skipped |
| `node --import tsx --test --test-name-pattern='Hybrid LoopCreate accepts a complete five-field cron' tests/extensions/audit-loop-monitor.test.ts` | Exit 0; 1 passed, 0 failed/skipped |
| `node --import tsx --test tests/extensions/audit-loop-monitor.test.ts` | Exit 0; 15 passed, 0 failed/skipped |
| `node --import tsx --test tests/extensions/loop-creation-recovery.test.ts tests/extensions/loop-interval-validation.test.ts` | Exit 0; 135 passed, 0 failed/skipped, including all 31 A03 and 104 A05 cases |
| `npm run typecheck` | Exit 0, `tsc --noEmit` clean |
| `npm test` | Exit 0; 758 passed, 0 failed/canceled/skipped/todo |
| `git diff --check` | Exit 0; clean |

An initial typecheck caught missing narrowing of the `beforeEach` context union in the new test file. Adding the same `assert.ok("mock" in t)` used by existing tests fixed it; the final typecheck and full suite above passed. No existing tests required fixture adaptation.

Eight unrelated `Known audit gap` cases remain in the audit file for A01, A02, A06, A08, A09, A10 and A11. Their passing results are defect characterizations, not evidence those findings are fixed. Historical audit files were not changed.

## Definition-of-done checklist

- [x] Formerly failing labeled full cron succeeds with all five fields and exact event source; demonstrated red before and green after.
- [x] Dedicated matrix covers full schedules, aliases/defaults, whitespace/source preservation, malformed grammar/schedules, options, event/timer paths, caps/orderings, cancellation, restoration, ordinary tools and schema validation. Existing audit coverage retains the 25-controller limit check.
- [x] Only the A07 `Known audit gap` block became a positive regression; unrelated coverage remains intact.
- [x] Grammar errors reject before snapshot/ID changes, persistence, registration or UI updates, with no success result.
- [x] Ordinary routing and strict schema fields/types/required properties remain unchanged; complete examples pass installed TypeBox `Check`, unknown properties fail.
- [x] Stored shape, journal behavior, defaults, read-only options, caps, lifetime and restoration remain compatible; no migration or independent cron parser.
- [x] README and tool description define examples, aliases/default source and limits; no hybrid slash-command route or cross-source debounce promise.
- [x] Targeted A07 and full audit tests pass; the original defect characterization is no longer the acceptance test.
- [x] Typecheck passes.
- [x] Full suite passes; unrelated defect characterizations are disclosed above.
- [x] Whitespace check passes. Worker diff review found only scoped production/tests/docs changes; no package or lock changes.
- [x] Scoped implementation commit and review PR target A05, with before/after results and this evidence. Actual commit, push and PR identifiers are supplied in the publication report and PR body, not inferred from test results.
- [ ] Independent coordinator/integration review remains pending. Worker diff review and 135 inherited regression passes show A03/A05 preservation; A06 remains the sole owner of cron semantics, with no scheduler/parser edits here. This is not independent acceptance.

Optional live smoke verification was omitted. A07 permits deterministic acceptance; there is no required live gate for this finding. No live panes or user sessions were touched. No exactly-once dispatch, crash durability or exhaustive regression-freedom claim is made.
