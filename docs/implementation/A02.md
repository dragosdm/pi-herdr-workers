# A02 implementation evidence

Date: 20 September 2026. Automated checks pass. Required live acceptance and a disposable CLI capture fixture are pending supervisor coordination.

- Specification: [A02](../specs/A02.md), ordered by the [spec index](../specs/README.md), specs PR [#11](https://github.com/dragosdm/pi-herdr-workers/pull/11).
- Worktree: `/Users/dragos/workspace/internal/pi-herdr-workers-fix-a02`.
- Branch and PR head: `fix/audit-a02`. Exact implementation parent and PR base: `fix/audit-a01` at `0ca320ee85ecc24c0961aac3f65fe37ca050d8bd`.
- Node: `v24.15.0`. No packages or lock files changed.
- Implementation commit: [`e03230a`](https://github.com/dragosdm/pi-herdr-workers/commit/e03230a). PR: [#18](https://github.com/dragosdm/pi-herdr-workers/pull/18). The following documentation commit records those links. Independent coordinator review and the live gate remain required. No merge is authorized.

## Baseline reproduction

Before changing production code, the new dedicated test required returned text and exact CLI arguments on the unchanged A01 base:

```text
node --import tsx --test --test-name-pattern='A02: ordinary terminal' tests/extensions/monitor-output-tail.test.ts
Exit 1; 1 test, 0 passed, 1 failed, 0 cancelled/skipped/todo.
SyntaxError: Unexpected token 'A', "AUDIT_SERVER_READY\n" is not valid JSON
```

The same positive regression passes after the implementation. It checks the actual returned row, `herdr` executable, explicit `--format text`, `recent-unwrapped`, five-row request, 15,000 ms timeout and propagated cancellation signal. The owned audit characterization now asserts `["AUDIT_SERVER_READY"]` and is named `Monitor readTail returns Herdr text output`. Every other audit assertion, including A01, is unchanged.

## Automated verification

Commands ran in bounded foreground processes in this worktree. Raw logs are not committed.

| Command | Result |
| --- | --- |
| `node --import tsx --test tests/extensions/monitor-output-tail.test.ts` | Exit 0; 49 tests, 49 passed, 0 failed/cancelled/skipped/todo |
| `node --import tsx --test tests/extensions/audit-loop-monitor.test.ts` | Exit 0; 15 tests, 15 passed, 0 failed/cancelled/skipped/todo |
| `npm run typecheck` | Exit 0; `tsc --noEmit`, no diagnostics |
| `npm test` | Exit 0; 688 tests, 688 passed, 0 failed/cancelled/skipped/todo |
| `git diff --check` | Exit 0; no whitespace errors |

Green `Known audit gap` tests still characterize unrelated defects. They do not establish fixes for other findings. The full suite includes all inherited A01 startup tests. No shared fixture or inherited assertion needed adaptation.

## Changed files and compatibility decisions

| File | Reason |
| --- | --- |
| `loop/runtime/herdr-monitor.ts` | Extract raw stdout execution while keeping JSON interpretation for metadata/mutations. Validate counts, request text and normalize display rows. Expose caller/lifetime cancellation checking to the list adapter. |
| `loop/tools/monitor-tools.ts` | Distinguish empty successful captures from failures and reject cancellation/disposal before widget/result publication. Only MonitorList changes. |
| `tests/extensions/monitor-output-tail.test.ts` | Dedicated real-manager and registered-tool regressions with injected exec, snapshots and deferred promises. |
| `tests/extensions/audit-loop-monitor.test.ts` | Convert only the A02 characterization into a returned-text regression; always dispose its manager. |
| `README.md` | Document snapshot limits, control cleanup, markers, fresh reload reads and observational meaning. |
| `docs/implementation/A02.md` | Record verification and remaining acceptance work. |

The raw helper preserves A01's timeout override, combined caller/lifetime signal and abortable execution. Metadata/mutation stdout still uses `stdout.trim() ? JSON.parse(stdout) : {}`. Malformed metadata remains an error; JSON-looking pane text remains literal. No fallback, retries, full-scrollback reads, stderr substitution or tail persistence were added.

The manager strips VT controls, normalizes CRLF/lone CR to LF, replaces remaining C0/C1 controls except LF with spaces, removes blank rows and keeps the last requested count. It does not truncate rows. The adapter retains five rows and 100 code units per displayed row, roster order, headers, counts, `displayRows()` and `textResult()` details. Only successful empty captures get `(no output captured)`; transport failures retain `(could not read pane)`.

A01's launch evidence and create/stop flow remain intact. No tool schema, snapshot interface, session scope, mutation queue, command identity, ownership check or tab/pane lifetime changed. A read failure does not alter status. Reads remain independent observations with no shared output state. Disposal and caller cancellation send no keys, close no pane, and start no later-pane read.

## Coverage and definition of done

Each item below corresponds to A02's definition of done, in order.

- [x] Dedicated regression file passes and asserts returned raw text, not merely the absence of an exception.
- [x] Exactly the A02 audit characterization is positive and renamed. No unrelated coverage was deleted or weakened.
- [x] Deterministic matrix covers exact argv, empty/JSON-looking text, control cleanup/Unicode, line boundaries and invalid counts, long output, successful stderr, nonzero/killed/rejected exec, environment guards, concurrent reads, recovery and restoration. Real-capture provenance is separately pending below.
- [x] Empty/whitespace/control-only captures return `[]` and one empty marker; actual `{}` remains visible.
- [x] Caller abort and manager disposal reject entry, pending reads/list, late exec success and publication. Deferred-exec cases verify the subprocess signal, no widget/result publication after pending cancellation and no later-pane reads or cancellation mutations. Cancellation triggered by widget execution is checked again before returning a result.
- [x] JSON metadata and empty mutation compatibility are tested, including real stop/refresh flow. Full inherited A01 create/stop tests pass.
- [x] Tool schemas and persistence formats remain unchanged. Snapshot round trips preserve all metadata, restore unknown status, read fresh text and launch nothing.
- [x] Targeted tests, typecheck, full suite and whitespace checks pass with the results above.
- [x] README documents the tail contract and markers. Historical audit report/evidence remain unchanged; no resolution note claims live acceptance.
- [ ] Required bounded Herdr 0.8.0 disposable run before and after reload, sanitized capture and completed cleanup. The worker has not run this live check or touched valuable user panes. Live compatibility is not yet accepted.
- [x] Diff is confined to A02 implementation, dedicated tests, the owned audit block and documentation. Dependencies, other worktrees/specifications and unrelated findings are untouched.
- [ ] Implementation commit is pushed and PR [#18](https://github.com/dragosdm/pi-herdr-workers/pull/18) targets A01, links A02/specs PR #11 and discloses live limitations. Independent coordinator review and resolution of any review findings remain pending, so the full reviewed-PR acceptance item is not yet satisfied.

Additional required fixture item: no approved disposable raw CLI capture was available when the automated tests were authored. Current stdout fixtures are synthetic terminal text, not JSON transport envelopes. The supervisor has been asked for a sanitized marker-only capture and provenance. Incorporating that capture as a fixture remains pending; neither historical audit IDs nor invented output are represented as raw captured evidence.

## Exact supervisor live check needed

Follow [A02's safe live procedure](../specs/A02.md#safe-live-verification-and-cleanup) in an approved disposable Herdr workspace/tab and Pi session using this branch. Record original inventory and created IDs; do not use `w6:p1` or `w6:p4`.

1. Call `MonitorCreate` once with a fresh unique suffix and bounded `printf` producing BEGIN, green READY and END marker rows. Verify actual execution before assessing output. If markers are absent, stop and report the A01 confounder rather than retrying.
2. Inspect only that pane's process state. Capture one direct `herdr pane read PANE_ID --source recent-unwrapped --lines 5 --format text`, stdout/stderr separately, and call `MonitorList` once. Compare normalized marker rows rather than shell prompts or whole-snapshot equality.
3. Reload only the disposable Pi session once. Call `MonitorList` once and verify the same handle returns fresh readable markers without another create or relaunch.
4. Retain CLI version, tested implementation revision, exact argv, sanitized marker capture/list results and provenance. Supply the sanitized direct stdout for a regression fixture. Label direct driver calls as bypassing model/schema validation.
5. Shut down the disposable session/driver, confirm shell readiness, close only recorded test-created resources and remove only the recorded private capture directory after retaining sanitized evidence. Confirm no user pane was renamed, interrupted or closed.

No polling, model loop, server or induced live timeout is needed. Optional unrelated live checks were omitted. These automated tests do not prove every terminal sequence, Herdr version or process race is safe; tails remain context, not application-readiness or completion evidence.
